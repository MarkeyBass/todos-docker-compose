https://www.youtube.com/watch?v=YW8VG_U-m48&ab_channel=JoranBeasley

create /ststic and /templates directory in /server root directory

Now - Conficure webpack to create the builds in the right place of the flask application.

💲 cd client
💲 npm run eject

--> config folder was created

--> paths.js - change buildPath 
      from: const buildPath = process.env.BUILD_PATH || 'build';
      to: const buildPath = process.env.BUILD_PATH || '../server/static/react';

---> webpack.config.js - 

replace 'static/' ocurences to ''

added fileName key to:
        new HtmlWebpackPlugin(
        Object.assign(
          {},
          {
            inject: true,
            template: paths.appHtml,
            filename: "../../templates/index.html"
          },


          -- this will copy index.html to the right place


add <script> to /client/public/index.html to interact with flask
-----------------------------------------------------------------
 @app.route("/")
def my_index():
    return Flask.render_template("index.html", flask_token="Hello Flask+React")

    <title>Todos App</title>
    <script>window.token = "{{flask_token}}"</script>

    now the tocken will be accessible from anywhere in our app

Add direction to the static files to package.json:
----------------------------------------
  "homepage": "/static/react",

💲 npm run build